#!/usr/bin/env node

var big = require('big');

var logger = big.logger,
    async = require('async');

// TODO remove hack by having starts run automatically after .use
async.series([
  function(callback) {
    big.use('http').start({path: __dirname + '/../public'}, callback);
  },
  function(callback) {
    big.use('auth-browserid').start({}, callback);
  },
  function(callback) {
    big.use('creature').start({}, callback);
  },
  function(callback) {
    big.use('space').start({}, callback);
  },
  function(callback) {
    big.use('un').start({path: __dirname + '/../view'}, callback);
  },
  function(callback) {
    big.use('admin').start({}, callback);
  }],
function(err, results) {
  if (err) { throw err; }
});
